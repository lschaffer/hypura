The raw log in Pic 3 is extremely illuminating. It reveals the exact three bugs inside the Hypura engine:

1. Bug #1: Hypura Parsed Tool Calls Inside the <think> Block (Cross-Contamination)
Look at what happened in Pic 3: Inside <think>...</think>, the model was analyzing:

"Wait, the prompt shows \n<tool_call>\n<function=get_devices_around_position>... twice? ... I'll just make one tool call properly: <function=get_coordinates><parameter=location>Göttingen</parameter></function>"

What Hypura did: Hypura's tool parser in src/server/streaming.rs:

Did not strip the <think>...</think> block before parsing.
Matched <function=get_devices_around_position> from the first thought and <parameter=location>Göttingen</parameter> from the second thought.
Combined them into get_devices_around_position(location: "Göttingen") ? which caused the Validation Error: Invalid parameter "location" in Pic 2!
Fix in Hypura (src/server/streaming.rs):
Strip <think>...</think> before running the tool call regex:

rust
// 1. Remove think blocks before searching for tool calls
let clean_text = if let Some(end_idx) = raw_text.find("</think>") {
    &raw_text[end_idx + "</think>".len()..]
} else if raw_text.starts_with("<think>") {
    "" // Still thinking, no tool calls outside think block yet
} else {
    &raw_text
};
// 2. Parse tool calls strictly on clean_text
2. Bug #2: The Tool Result JSON Was Not Delivered into the Prompt
Look at what Qwen explicitly wrote in its internal reasoning (Pic 3):

"Wait, the system response shows 'Called tool: get_devices_around_position' with arguments, but no actual data returned yet in the prompt? Ah, the prompt ends right after the tool call block."

Why this happened in Hypura (src/server/chat.rs): When the client sent role: "tool" with the 2924-char JSON result:

Hypura either skipped the role: "tool" message in its prompt builder, or placed the placeholder title ("Called tool: get_devices_around_position...") instead of the actual message.content (the JSON data).
Because the model saw the tool call was made but saw zero returned data, it assumed it was in a fresh conversation and started over.
Fix in Hypura (src/server/chat.rs):
Ensure role: "tool" is rendered with its full JSON content:

rust
match msg.role.as_str() {
    "tool" => {
        // Must insert the actual JSON content inside <tool_response>
        prompt.push_str(&format!(
            "<|im_start|>user\n<tool_response>\n{}\n</tool_response><|im_end|>\n",
            msg.content
        ));
    }
    // ...
}
3. Bug #3: History Ending with Assistant Turn Instead of Waiting for Final Answer
Ensure that after appending the <tool_response>, Hypura always appends the assistant generation prompt trigger:

rust
// At the very end of format_qwen_chat_prompt:
prompt.push_str("<|im_start|>assistant\n");
This primes Qwen to immediately summarize the <tool_response> rather than creating new tool calls.

Summary Checklist for Hypura:
Priority	File	Fix Required
P0	src/server/streaming.rs	Strip <think>...</think> before running tool call regex matching.
P0	src/server/chat.rs	Ensure role: "tool" injects msg.content (the JSON data) into <tool_response>.
P1	src/server/chat.rs	End prompt with `<
