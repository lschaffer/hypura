Based on Pic 3 (the Hypura implementation) and Pic 1 & Pic 2 (the test run), here are the exact reasons why the problem is still occurring and how to fix it in Hypura:

Why the Issue Still Happens (3 Flaws in Hypura)
1. Mistral Prompt Formatter Was Not Updated (Only format_qwen_chat_prompt was changed)
In Pic 3, the tool response formatting was only implemented in format_qwen_chat_prompt using <|im_start|>user\n<tool_response>....
The model in your test is ministral3:14b (Mistral / Tekken architecture).
Ministral does not use Qwen's ChatML template. It is routed to format_mistral_chat_prompt (or a fallback formatter) in src/server/chat.rs, where role: "tool" is still not formatted correctly for Mistral.
When Ministral receives the tool output without Mistral's native [TOOL_RESULTS] tokens, it ignores the data and restarts from the first tool (getgroupid).
2. Multi-Token Stop Word Failure in src/compute/inference.rs
Look at Pic 1 & Pic 2: </assistant> </assistant> is still being emitted in the output.
Why didn't Hard Token Generation Stop catch </tool_call> or </assistant>?
</tool_call> and </assistant> are multi-token strings (e.g. ['<', '/tool', '_call', '>']).
If inference.rs only checks token_id == stop_token_id (single token ID comparison), it will never match multi-token strings.
In llama.cpp / Rust inference, stop strings must be checked against a sliding text buffer of decoded tokens (or registered in the llama sampling chain).
3. Ministral 3 Native Tool Template vs. <assistant> Tags
The model output in your screenshot shows:
text
<tool_call>
{"name": "get_devices_around_position", ...}
</tool_call>
</assistant> </assistant>
The </assistant> tag is coming from Hypura's prompt wrapping. Because Mistral was trained on [INST] / [/INST] rather than <assistant> tags, it gets confused, treats <assistant> as text to complete, and generates closing tags in a loop.
Exact Solutions for Hypura Engine
Fix 1: Implement format_mistral_chat_prompt in src/server/chat.rs
For models containing mistral, ministral, codestral, or nemo:

rust
// For role == "tool" / "tool_response" in Mistral:
// Mistral Instruct v3 / Tekken format:
format!("[TOOL_RESULTS] {{\"content\": {}}} [/TOOL_RESULTS]", message.content)
And for prompt construction:

rust
// User with Tool Result -> Assistant Turn
"[INST] [TOOL_RESULTS] " + &tool_content + " [/TOOL_RESULTS] [/INST]"
If using prompt-based XML tools for Mistral:

rust
"[INST] TOOL RESULT:\n" + &tool_content + "\n\nSummarize the result and answer the user query. [/INST]"
Fix 2: Buffer-Based Stop Checking in src/compute/inference.rs
In generate_from_loaded / generate_blocking, maintain a recent text window and check if it contains the stop string:

rust
// In your token generation loop in inference.rs:
let mut generated_text = String::new();
let stop_sequences = ["</tool_call>", "</tool__call>", "</toolcall>", "</assistant>", "<|im_end|>", "</s>"];
// On each new token decoded into `piece`:
generated_text.push_str(&piece);
for stop_str in &stop_sequences {
    if generated_text.ends_with(stop_str) || generated_text.contains(stop_str) {
        // Truncate the stop string if necessary and break generation immediately
        break;
    }
}
Fix 3: Dynamic Model Routing in src/server/chat.rs
Ensure ministral models are correctly identified and dispatched:

rust
if model_name.to_lowercase().contains("qwen") {
    format_qwen_chat_prompt(&messages)
} else if model_name.to_lowercase().contains("mistral") || model_name.to_lowercase().contains("ministral") {
    format_mistral_chat_prompt(&messages)
} else {
    format_generic_chat_prompt(&messages)
}
7:59 AM
